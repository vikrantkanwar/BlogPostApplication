#Global Data Source | Global Configuration -> Connect to PostgreSql

spring.datasource.url = jdbc:postgresql://localhost:5432/technicalblog
spring.datasource.username = postgres
spring.datasource.password = vikrantkanwar

#The SQL Dialect -> Makes SQL queries faster and efficient + Hibernate to manage the db
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

#Authorization to Hibernate to perform DLL operations - > create, update, delete
spring.jpa.hibernate.ddl-auto = create
